@model IEnumerable<Summ2.Models.tblItemCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tblCategory.CategoryName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblItem.ItemName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StatusBeschrijving)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tblCategory.CategoryName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblItem.ItemName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StatusBeschrijving)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.ItemCategoryID }) |
            @Html.ActionLink("Details", "Details", new { id = item.ItemCategoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ItemCategoryID })
        </td>
    </tr>
}

</table>
